# Guacamole HTML5 web client -- the official image directly, configured
# entirely via env vars (it bundles the JSON auth extension already). Set the
# secret with `fly secrets set -a niksenflow-guacamole-fra JSON_SECRET_KEY=...`
# before first deploy -- see README.md.
app = "niksenflow-guacamole-fra"
primary_region = "fra"

[build]
  image = "guacamole/guacamole:1.5.5"

[env]
  GUACD_HOSTNAME = "niksenflow-guacd-fra.internal"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  size = "shared-cpu-2x"
  memory = "1024mb"
